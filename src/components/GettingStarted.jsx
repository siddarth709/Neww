import React,{useState} from "react";
const COMMAND="python train_real.py --config configs/mnist_cnn.yaml";
export default function GettingStarted(){const[copied,setCopied]=useState(false);const copy=async()=>{try{await navigator.clipboard.writeText(COMMAND);setCopied(true);setTimeout(()=>setCopied(false),1600)}catch{}};return <section className="start-panel"><div className="start-icon"><span className="loader-ring"/></div><div className="start-copy"><span className="panel-kicker">READY TO MONITOR</span><h2>No training activity yet</h2><p>Blockchain verification is online. Launch the federated training process to stream gradient commits, anomaly scores and node reputation changes into this dashboard.</p><div className="command"><span>$</span><code>{COMMAND}</code><button onClick={copy}>{copied?"COPIED":"COPY"}</button></div></div></section>}
