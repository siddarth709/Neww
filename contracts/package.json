{
    "name": "training-verification-contracts",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "node": "hardhat node",
        "compile": "hardhat compile",
        "deploy:local": "hardhat run scripts/deploy.js --network localhost",
        "register:local": "hardhat run scripts/registerNodes.js --network localhost",
        "test": "hardhat test",
        "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
        "register:sepolia": "hardhat run scripts/registerNodes.js --network sepolia"
    },
    "devDependencies": {
        "hardhat": "^2.22.10",
        "@nomicfoundation/hardhat-toolbox": "^5.0.0"
    }
}
